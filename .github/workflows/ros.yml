# https://github.com/ros-tooling/setup-ros
name: ros
on: [push, workflow_dispatch, pull_request]
jobs:
    ros:
      runs-on: ${{ matrix.os }}
      strategy:
        matrix:
          os: ['ubuntu-24.04', 'ubuntu-24.04-arm']
      container:
        image: murilomarinho/sas_ros_jazzy:latest
      steps:
        - name: Build colcon
          run: |
            colcon build

